[tox]
envlist = ruff, ty
isolated_build = True

[testenv:ruff]
deps =
    ruff==0.15.22
changedir = {toxinidir}
commands =
    ruff check .

[testenv:ty]
deps =
    ty>=0.0.66
changedir = {toxinidir}
commands =
    ty check .
